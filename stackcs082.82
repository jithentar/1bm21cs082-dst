#include <stdio.h>
# define STACK_SIZE 5
int st[10] top,item,top=-1;
void push()
{
 if(top==STACK_SIZE)
prinyf("stack over flow\n");
else
{
    top++;
    srt[top]=item;
}
}
int pop()
{
    int del.item;
    if(top==-1)
    printf("stack undefined\n");
    else
    {
        del_item=st[top]
        top--;
    }
}
void display()
{
 int i;
 if(top==-1)
 printf("stack is empty\n");
 for(i=0;i<=top;i++)
 printf("%d\t",st[i]);
}
 void main()
 {
    int x;
    printf("enter the choice\n");
    scanf("%d",&choice);
    while(1)
    switch(choice)
    {
        case1:printf(" ");
        scanf("%d",&item);
        push();
        break;
        case2:x=pop()
        printf("%d",x);
        break;
        case3:display()
        break;
        default: exit(0);


}
 }